'use client';

import UserNotifications from '@/modules/dashboard/(modules)/users/(modules)/notifications/UserNotifications';

export default function NotificacionesPage() {
    return <UserNotifications />;
}
